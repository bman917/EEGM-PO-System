<%=javascript_tag do %>

$(document).ready(function(){

   $('#purchase_order_supplier_name').autocomplete({
    source: $('#purchase_order_supplier_name').data('autocomplete-source'),
    minLength: 2
  });

});
<% end %>

  <div class="header">

    <%=image_tag "EEGM.png", class: "logo"%> 
    <%=image_tag "ESS.png", class: "logo"%> 

      <div class="float_right">
      #113 (old) J.P. Rizal St., Calumpang, Marikina City, Philippines 1801
      <span class="highlight">ENGSHOESUPPLY@GMAIL.COM</span>
      <span class="highlight">WWW.ENGSHOESUPPLY.COM</span>
      <span class="phone">Tel: 6453017</span>
      <span class="phone">Fax: 6452210</span>
      </div>
  </div>

<div class="section">
<h1 class="emphasize med_text indented">Purchase Orders</h1>

<table id="po_table">
  <thead>
    <tr>
      <th></th>
      <th class="small_column">Order Date</th>
      <th>Supplier</th>
      <th>Total</th>
      <th>Description</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @purchase_orders.each do |purchase_order| %>
      <tr>
        <td class="smaller_column"><%= link_to 'View', purchase_order %></td>
        <td><%= purchase_order.po_date %></td>
        <td><%= purchase_order.supplier %></td>
        <td class="small_column"><%= purchase_order.grand_total_formatted %></td>
        <td class="description_column"><%= truncate purchase_order.items_description, length: 70 %></td>
        <td><%= link_to 'X', purchase_order, class: 'remove_fields', method: :delete, data: { confirm: "Delete PO for '#{purchase_order.supplier_name}'?" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>

<div class="section indented">

<%= form_for(@new_purchase_order) do |f| %>

  <% if @new_purchase_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_purchase_order.errors.count, "error") %> prohibited this Purchase Order from being saved:</h2>

      <ul>
      <% @new_purchase_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<h2 class="emphasize med_text">New Purchase Order</h2>
  <div  class="sub_section">
    <%= f.label "Order To:" %><%= f.text_field :supplier_name, tabindex: 1, data: {autocomplete_source: supplier_names_path} %>
    <%= f.hidden_field :po_date, tabindex: 2%>
    <%= f.submit "Create"%>
  </div>

<% end %>
</div>